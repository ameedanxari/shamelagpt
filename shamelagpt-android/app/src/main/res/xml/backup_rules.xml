<?xml version="1.0" encoding="utf-8"?>
<!-- Exclude session_prefs so reinstall cannot restore a logged-in session (API < 31). -->
<full-backup-content>
    <exclude domain="sharedpref" path="session_prefs" />
</full-backup-content>
