Replace these placeholder files with real font files.

Instructions:
- Add licensed font files (TTF/OTF) for the languages you want to support.
- Recommended fonts:
  - NotoNaskhArabic-Regular.ttf (Arabic)
  - NotoNastaliqUrdu-Regular.ttf (Urdu)
  - NotoNaskhArabic-Persian.ttf (Persian variant if needed)
  - NotoNaskhSindhi-Regular.ttf (Sindhi)
- After adding the real font files, update `Info.plist` `UIAppFonts` entries if filenames differ.

Do NOT commit proprietary fonts unless you have the rights to do so.
